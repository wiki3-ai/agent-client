"""Builtin notebook templates shipped with agent-kernel.

Templates are loaded via ``importlib.resources``. To add a template, drop a
``<name>.ipynb`` file into this directory and register the name in
``agent_kernel.runtime.template_registry._BUILTIN_TEMPLATES``.
"""
