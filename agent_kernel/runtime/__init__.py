"""agent_kernel.runtime: scheduler, notebook runner, spawn, reconstruction.

Modules in this package are filled in progressively per milestone:
- ``reconstruct``       — M1 (event-stream → state)
- ``notebook_runner``   — M2
- ``materializer``      — M3
- ``parameter_injection``— M3
- ``template_registry`` — M3
- ``scheduler``         — M4
- ``policy_engine``     — M4
- ``spawn_manager``     — M5
"""
